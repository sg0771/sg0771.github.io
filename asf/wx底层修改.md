# 修复问题
1.  修复硬编码在自动码率时初始化失败的问题 (改部分代码)

2.  将视频采集和编码从同一个线程拆分到不同线程，优化录屏帧率 (需要改结构)

3.  增加录屏时的错误提示，包括 路径不可写、参数错误、创建编码器失败，结束文件错误等 (改部分代码)

4.  增加音频试听功能（音频文件循环播放，麦克风测试监听） (增加代码)

5.  视频临时文件从flv改成wxm加密格式  (增加部分代码，替换libffmpeg.dll)

6.  解决音视频录制不同的问题  （改部分代码结构）

7.  增加对默认通信设备的监听和采集功能(采集时指定设备名字"conf")

8.  增加麦克风回音消除功能 （改部分代码结构）

9.  增加多屏幕录制功能(可以选择所有屏幕录制为一个视频文件，或者选择多个不同的显示器名字录制成多个不同的视频文件)（改部分代码结构


# 1.5.9 版本功能和问题

教育场景(录屏叠加摄像头图像):  录制屏幕时，指定摄像头显示宽度，并同时打开摄像头设备，录屏后将摄像头图像叠加到视频图像中

问题：

引进openmp 导致依赖VS运行时库 vcomp140.dll导致的启动失败，需要在纯净场景中测试，或者帮助用户安装Windows 的C++运行时库


新功能开发

多屏录制： 屏幕录制时，扩展录屏设备名，同时录制多个屏幕内容为多个视频文件

GIF优化: ffmpeg 本身的GIF时统一调试办，在大多数录屏时会产生栅格化，优化GIF算法产生高清GIF图像(但是比较慢)

蓝牙音频处理优化:  Win11 以下的蓝牙音频设备存在立体声模式和免提模式冲突，需要指定禁用其中一种

游戏场景优化：优化录制和编码流程


功能优化：
回声消除：移植webrtc算法，在常见的笔记本电脑上麦克风录音效果还可以
美白算法研究： 基于OpenCV开源实现
音视频不同步问题优化: 找到问题并解决
视频临时文件处理： 使用基于TS的修改进行异步输出