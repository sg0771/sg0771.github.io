# 一、编译器相关（visual studio，xcode）
1.1问题：error LNK2001: unresolved external symbol __imp__unlink
解决方法：As predicted, one of the third-party libraries was not set to /MT in release (but it was /MTd in debug) - changing that flag made it all work.

1.2.问题：用VS2015导入VS2013的工程的时候，编译出现了error LNK2001: 无法解析的外部符号 __vsnprintf的错误
解决方法：
出现这个问题的原因是vs2015默认编译时将许多标准库采用内联方式处理，因而没有可以链接的标准库文件，所以要专门添加标准库文件来链接标准库中的函数。在附加依赖项中添加legacy_stdio_definitions.lib库

1.3.问题：error LNK2038: 检测到“_ITERATOR_DEBUG_LEVEL”的不匹配项: 值“2”不匹配值“0”
解决方法：项目-〉属性-〉配置属性-〉C/C++ -〉预处理器-〉预处理定义 -〉添加“_ITERATOR_DEBUG_LEVEL=0”即可。

1.4.问题：error LNK2019: 无法解析的外部符号 __imp____iob_func
解决方法：代码里加下面
#if _MSC_VER>=1900
#include "stdio.h" 
_ACRTIMP_ALT FILE* __cdecl __acrt_iob_func(unsigned); 
#ifdef __cplusplus 
extern "C" 
#endif 
FILE* __cdecl __iob_func(unsigned i) { 
    return __acrt_iob_func(i); 
}
#endif /* _MSC_VER>=1900 */

1.5.问题：error LNK2026: 模块对于 SAFESEH 映像是不安全的
解决方法：
1.打开该项目的“属性页”对话框。
2.单击“链接器”文件夹。
3.单击“命令行”属性页。
4.将 /SAFESEH:NO 键入“附加选项”框中，然后点击应用。

1.6.问题：linux/unix平台下结构体对齐问题
解决方法：
#pragma pack(1)

1.7 warning C4251: “Test::m_objCon”: class“std::vector<_Ty>”需要有 dll 接口由 class“Test”的客户端使用
解决方法：
https://blog.csdn.net/shellching/article/details/7090742
这通常是由于以数据成员方式在DLL导出类中使用了模板类造成的
这个问题主要要描述的是不同的地方的vector的实现可能不一样所造成的问题。所以我们应该将其导出。有很多方法可以解决这个问题的。

第一种： 无视它或者#pragma warnind( disable: 4251 )
第二种：将该数据改为指针方式：
class __declspec( dllexport ) Test
{
public:
std::vector<int>* m_objCon;
};
然后在构造函数和析构函数中分别初始化和释放它。
第三种：
将该模板类及其依赖类导出。
#include <iostream>
#include <vector>
using namespace std;

class __declspec( dllexport ) Test
{
public:
template  class __declspec( dllexport ) std::allocator<int>;
template  class __declspec( dllexport ) std::vector<int, std::allocator<int> >;
public:

std::vector<int> m_objCon;
};
这种方法要注意的是必须要把要导出模板类的模板数据成员一并导出。有点类似于显式实例化。比如说你要导出boost::shared_ptr就还必须将其依赖的shared_count一并导出。导出map还需要导出对应pair等等。很麻烦啦~所以我们还是选择第四种吧。

第四种：Impl。
#include <iostream>
#include <vector>
using namespace std;

// 这些放到.h中
class Test_imp;
class __declspec( dllexport ) Test
{
// 构造函数中初始化 析构中释放m_pImp;
void test();
public:
Test_imp* m_pImp;
};

class  Test_imp
{
public:
void test(){}
std::vector<int> m_objCon;
};

// 放到cpp中
void Test::test()
{
m_pImp->test();
}

1.8 Visual Studio VS如何统计代码行数
编辑-查找和替换-在文件中查找，然后查找内容填写下面的东西，勾选使用正则表达式，点击查找全部
b*[^:b#/]+.*$
在查找结果的最后一行显示了总的行数和文件数

1.9 xcode 10使用cmake编译报错问题
暂时使用xcode 9及以下版本进行编译。


# 二、语言相关（c/c++，c#，python，java）
2.1 c/c++ 字符串赋值，小心被截断造成数据不完整
如：
char buffer[10];//
string str = buffer;//如果buffer中的数据中间有'\0'，则str会保存截断后的buffer中数据。建议用assign等方式来赋值。

2.2 引用DLL中导出的全局变量
方法：
extern int _declspec(dllimport) dllGlobalVar;

2.3 清空stdin缓冲区
方法：
setbuf(stdin, NULL);
scanf(...);
设置不带缓冲区，Linux下有效。
windows下，可以用fflush(stdin);

2.4 类静态成员变量的导出问题
方法：
_declspec(dllexport)与_declspec(dllimport)
https://blog.csdn.net/slshelly/article/details/8720487

2.5 计时方法 c++11语法可用，跨平台
chrono::steady_clock::time_point end_encode = chrono::steady_clock::now();
	long tileEncodeTime = chrono::duration_cast<chrono::milliseconds>(end_encode - begin_encode).count();

2.6 C++ delete空指针
delete 指针之前，要有判空操作，不同平台或者编译器对空指针的删除采用方案可能不同，导致删除卡住崩溃：
例如Android开发采用Android Studio 删除空指针就会卡住导致崩溃。

# 三、设计模式相关
3.1单例模式
饿汉模式和饱汉模式，注意线程安全

3.2策略模式
组合比继承重要

3.3工厂模式
工厂方法和抽象工厂

3.4观察者模式
拉模式和推模式


# 四、跨平台开发相关（windows，mac，android，linux）
4.1.ubuntu64位下编译glib库


4.2 Windows命令查看文件MD5
certutil -hashfile yourfilename.ext MD5
certutil -hashfile yourfilename.ext SHA1
certutil -hashfile yourfilename.ext SHA256

4.3 cmake模块对于SAFESEH映像是不安全
//模块对于SAFESEH映像是不安全
set(CMAKE_EXE_LINKER_FLAGS "${CMAKE_EXE_LINKER_FLAGS} /SAFESEH:NO")
set(CMAKE_SHARED_LINKER_FLAGS "${CMAKE_SHARED_LINKER_FLAGS} /SAFESEH:NO")
set(CMAKE_MODULE_LINKER_FLAGS "${CMAKE_MODULE_LINKER_FLAGS} /SAFESEH:NO")

//添加预处理器定义
ADD_DEFINITIONS(-D_CRT_SECURE_NO_WARNINGS)
ADD_DEFINITIONS(-D_WINSOCK_DEPRECATED_NO_WARNINGS)

4.4 开发使用boost库出错
Undefined symbols for architecture x86_64:
  "boost::system::detail::generic_category_instance", referenced from:
      boost::system::generic_category()
解决方案：
增加宏
BOOST_NO_CXX14_CONSTEXPR

4.5 xcode编译
xcode10以后编译总是会出现一些问题。建议用xcode9及以前版本编译。